# Install the following latex dependencies:

	texlive-algorithmicx
	texlive-units

# To compile the pdf:

	Go in the folder "latex" and run "make".

# The folder "submission" contains the paper submission and reviewers' comments.

